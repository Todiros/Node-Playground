<!DOCTYPE html>
<html lang="en">
  <head>
    {{>head}}
    <title> {{title}} </title>
  </head>
  <body>
    <div class="header-wrapper">
      {{>header}}
    </div>
    <div class="content-wrapper">
      {{{body}}}
    </div>
    {{>scripts}}
  </body>
</html>