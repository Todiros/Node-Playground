<!DOCTYPE html>
<html lang="en">
  <head>
    {{>head}}
    <title> {{title}} </title>
  </head>
  <body>
    {{>header}}
    <div class="content-wrapper">
      {{{body}}}
    </div>
    {{>scripts}}
  </body>
</html>